databases:
  - name: pg_db
    region: frankfurt
    plan: free

services:
  - type: web
    name: spring-dockerized-service
    runtime: docker
    region: frankfurt
    plan: free
    healthCheckPath: /
